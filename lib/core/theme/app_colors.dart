import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Palette
  static const primary = Color(0xFF134E4A); // Deep Teal
  static const primaryTeal = primary;
  static const primaryLight = Color(0xFF1E6D68);
  static const primaryDark = Color(0xFF0D3432);

  static const gold = Color(0xFFC5A059); // Muted Gold
  static const accentGold = gold;

  // Neutral Colors
  static const background = Color(0xFFF9FAFB);
  static const backgroundWhite = background;

  static const surface = Color(0xFFF3F4F6);
  static const surfaceGray = surface;

  static const textPrimary = Color(0xFF111827);
  static const textDark = textPrimary;

  static const textSecondary = Color(0xFF4B5563);
  static const textHint = Color(0xFF9CA3AF);
  static const error = Color(0xFFDC2626);
  static const white = Colors.white;
}
